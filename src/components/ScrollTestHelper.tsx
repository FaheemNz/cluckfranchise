"use client";

import { useScrollToTopVercel } from "@/src/hooks/useScrollToTop";

/**
 * Helper component to test scroll behavior on individual pages
 */
const ScrollTestHelper = () => {

    useScrollToTopVercel();

    return null;
};

export default ScrollTestHelper;